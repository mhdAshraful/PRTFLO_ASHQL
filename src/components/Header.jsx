import React from 'react'
import NavBar from './NavBar'



export const Header = ({ width }) => {
    return (
        <header id='header'>
            <NavBar width={width} />
        </header>
    )
}
